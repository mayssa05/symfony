<style>
    /* Styles pour la table */
    table {
        width: 100%;
        border-collapse: collapse;
        background-color: #a09393;
    }

    /* Styles pour l'en-tête de la table */
    th {
        background-color: #1f0f86;
        color: white;
        font-weight: bold;
        padding: 10px;
    }

    /* Styles pour les cellules du tableau */
    td {
        padding: 10px;
        border: 1px solid #131313; /* Couleur du trait plus claire */
    }

    /* Styles pour les cellules des lignes impaires */
    tr:nth-child(odd) {
        background-color: #e1e1e1;
    }

    /* Style pour le trait entre "Edit" et "Delete" */
    .action-separator {
        border-right: 1px solid #eee; /* Couleur du trait plus claire */
        margin-right: 5px;
        margin-left: 5px;
        height: 15px;
    }

    /* Styles pour les liens au survol de la souris */
    td a:hover {
        text-decoration: underline;
    }
</style>

{{ form_start(f) }}
{{ form_widget(f) }}
{{ form_end(f) }}
<table border="3">
    <tr>
        <th>ref</th>
        <th>title</th>
        <th>publictiondate</th>
        <th>published</th>
        <th>author name</th>
        <th>action</th>

    </tr>
    {% for i in book %}
    <tr>
        <td>{{ i.ref }}</td>
        <td>{{ i.title }}</td>
        <td>{{ i.publicationDate |date()}}</td>
        <td>{{ i.published}}</td>
        <td>{{ i.author.username }}</td>

         <td>
        <a href="{{path('editbook',{'ref':i.ref})}}" class="editbook-link">edit</a>
        </td>
        <td>
        <a href="{{path('deletbook',{'ref':i.ref})}}" class="deletbook-link">delet</a>
        </td>
        <td>
        <a href="{{path('addbook',{'ref':i.ref})}}" class="addbook-link">add</a>
        </td>
    </tr>
{% endfor %}

</table>
